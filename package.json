{
  "name": "neynar_gm_bot",
  "version": "0.0.0",
  "description": "A bot that will cast a 'Good Morning...' every morning",
  "main": "./dist/app.js",
  "type": "module",
  "scripts": {
    "watch": "tsc --watch",
    "build": "rm -rf dist && tsc",
    "dev": "npm run build && pm2 start ecosystem.config.cjs"
  },
  "author": "Neynar",
  "license": "ISC",
  "dependencies": {
    "@neynar/nodejs-sdk": "^0.10.5",
    "@types/node": "^20.9.0",
    "chatgpt": "^5.2.5",
    "dotenv": "^16.3.1",
    "node-cron": "^3.0.3",
    "typescript": "^5.2.2"
  },
  "devDependencies": {
    "@types/node-cron": "^3.0.11",
    "ts-node": "^10.9.1"
  }
}
